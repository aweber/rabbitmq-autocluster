{
    "check": {
        "name": "RabbitMQ HTTP",
        "notes": "Use curl to check the web service every 10 seconds",
        "script": "curl -s http://guest:guest@192.168.150.10:15672/api/aliveness-test/%2f",
        "interval": "10s"
    }
}
