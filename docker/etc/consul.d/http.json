{
    "check": {
        "name": "RabbitMQ HTTP",
        "notes": "Use curl to check the web service every 10 seconds",
        "script": "curl http://localhost:15672/api/aliveness-test/%2f",
        "interval": "10s"
    }
}
